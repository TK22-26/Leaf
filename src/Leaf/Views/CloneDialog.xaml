<Window x:Class="Leaf.Views.CloneDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Leaf.Converters"
        Title="Clone Repository"
        Height="550"
        Width="700"
        MinHeight="450"
        MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Clone Repository"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <!-- Tab Control for Browse/URL -->
        <TabControl Grid.Row="1" x:Name="ModeTabControl" SelectionChanged="ModeTabControl_SelectionChanged">
            <!-- Browse Azure DevOps Tab -->
            <TabItem Header="Browse Azure DevOps">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Refresh Button and Status -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="AzureStatusText"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                        <Button Grid.Column="1"
                                x:Name="RefreshButton"
                                Content="Refresh"
                                Padding="12,4"
                                Click="Refresh_Click"/>
                    </Grid>

                    <!-- Search Box -->
                    <Grid Grid.Row="1" Margin="0,0,0,8">
                        <TextBox x:Name="SearchTextBox"
                                 Padding="8,6"
                                 TextChanged="SearchTextBox_TextChanged"/>
                        <TextBlock x:Name="SearchPlaceholder"
                                   Text="Search repositories..."
                                   IsHitTestVisible="False"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                    </Grid>

                    <!-- Repository List -->
                    <Border Grid.Row="2"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}">
                        <Grid>
                            <ListBox x:Name="RepoListBox"
                                     SelectionChanged="RepoListBox_SelectionChanged"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="4,6">
                                            <TextBlock Text="{Binding DisplayName}"
                                                       FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding RemoteUrl}"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <!-- Loading Overlay -->
                            <Border x:Name="LoadingOverlay"
                                    Background="#80FFFFFF"
                                    Visibility="Collapsed">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                                    <TextBlock Text="Loading repositories..."
                                               HorizontalAlignment="Center"
                                               Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Empty State -->
                            <TextBlock x:Name="EmptyStateText"
                                       Text="No repositories found. Configure your organization and PAT in Settings."
                                       TextWrapping="Wrap"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Manual URL Tab -->
            <TabItem Header="Enter URL">
                <StackPanel Margin="8">
                    <TextBlock Text="Repository URL"
                               Margin="0,0,0,4"
                               FontWeight="SemiBold"/>
                    <TextBox x:Name="UrlTextBox"
                             Padding="8,6"
                             TextChanged="UrlTextBox_TextChanged"/>
                    <TextBlock x:Name="UrlHintText"
                               Text="Enter a Git repository URL (HTTPS)"
                               FontSize="11"
                               Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <!-- Selected Repository Info -->
        <Border x:Name="SelectedUrlBorder"
                Grid.Row="2"
                BorderThickness="1"
                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,12,0,0"
                Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="URL: " FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="SelectedUrlText"
                           Grid.Column="1"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </Grid>
        </Border>

        <!-- Destination Path -->
        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Text="Clone to"
                       Margin="0,0,0,4"
                       FontWeight="SemiBold"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="DestinationTextBox"
                         Grid.Column="0"
                         Padding="8,6"
                         TextChanged="DestinationTextBox_TextChanged"/>
                <Button Grid.Column="1"
                        Content="Browse..."
                        Padding="12,6"
                        Margin="8,0,0,0"
                        Click="BrowseDestination_Click"/>
            </Grid>
            <TextBlock x:Name="RepoNameText"
                       FontSize="11"
                       Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                       Margin="0,4,0,0"/>

            <!-- Progress -->
            <StackPanel Margin="0,8,0,0">
                <ProgressBar x:Name="CloneProgressBar"
                             Height="6"
                             IsIndeterminate="True"
                             Visibility="Collapsed"/>
                <TextBlock x:Name="ProgressText"
                           FontSize="11"
                           Margin="0,4,0,0"
                           Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
            </StackPanel>
        </StackPanel>

        <!-- Footer buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="CloneButton"
                    Content="Clone"
                    Padding="16,8"
                    IsDefault="True"
                    Click="Clone_Click"/>
            <Button Content="Cancel"
                    Padding="16,8"
                    Margin="8,0,0,0"
                    IsCancel="True"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
