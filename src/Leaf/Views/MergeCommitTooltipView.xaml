<UserControl x:Class="Leaf.Views.MergeCommitTooltipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Leaf.Controls"
             xmlns:converters="clr-namespace:Leaf.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="240" d:DesignWidth="420">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:OverflowCommitCountConverter x:Key="OverflowCommitCountConverter"/>
    </UserControl.Resources>

    <Grid Height="{Binding TotalHeight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <controls:GitGraphCanvas Grid.Column="0"
                                 Grid.Row="0"
                                 Nodes="{Binding Nodes}"
                                 MaxLane="{Binding MaxLane}"
                                 RowHeight="{Binding RowHeight}"
                                 Height="{Binding GraphHeight}"
                                 MinWidth="60"
                                 LabelAreaWidth="0"
                                 IsHitTestVisible="False"
                                 VerticalAlignment="Top"/>

        <ItemsControl Grid.Column="1"
                      Grid.Row="0"
                      ItemsSource="{Binding VisibleCommits}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="{Binding DataContext.RowHeight, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Margin="8,0,12,0"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis">
                                <Run Text="{Binding MessageShort, Mode=OneWay}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <Run Text=" " Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <Run Text="{Binding Description, Mode=OneWay}" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                            </TextBlock>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding DateDisplay}"
                                       Margin="0,0,8,0"
                                       FontSize="11"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        <Grid Grid.Column="1"
              Grid.Row="1"
              Margin="8,0,8,0"
              Height="{Binding RowHeight}"
              Visibility="{Binding HasOverflow, Converter={StaticResource BooleanToVisibilityConverter}}"
              VerticalAlignment="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{Binding OverflowCount, Converter={StaticResource OverflowCommitCountConverter}}"
                           FontSize="11"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
            </Grid>
        </Grid>
</UserControl>
