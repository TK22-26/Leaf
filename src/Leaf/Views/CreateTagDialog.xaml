<Window x:Class="Leaf.Views.CreateTagDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Tag"
        Width="360"
        Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tag name" FontWeight="SemiBold"/>
        <TextBox x:Name="TagNameTextBox"
                 Grid.Row="1"
                 Margin="0,6,0,12"
                 TextChanged="TagNameTextBox_TextChanged"/>

        <TextBlock Grid.Row="2"
                   Text="Message (optional)"
                   FontWeight="SemiBold"/>
        <TextBox x:Name="TagMessageTextBox"
                 Grid.Row="3"
                 Margin="0,6,0,12"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Width="80"
                    Margin="0,0,8,0"
                    IsEnabled="False"
                    Click="OkButton_Click">
                Create
            </Button>
            <Button Width="80" Click="CancelButton_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
