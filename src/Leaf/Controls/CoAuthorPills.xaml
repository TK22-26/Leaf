<UserControl x:Class="Leaf.Controls.CoAuthorPills"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Leaf.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="Root">
    <!--  Inline avatar-only pills for co-authors. Each pill is a small
          rounded chip carrying an identicon plus a per-pill tooltip
          (name + email). The whole control collapses when CoAuthors
          is empty so the parent layout doesn't carry blank space.  -->
    <ItemsControl ItemsSource="{Binding CoAuthors, ElementName=Root}">
        <ItemsControl.Style>
            <Style TargetType="ItemsControl">
                <Setter Property="Visibility" Value="Visible" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding HasCoAuthors, ElementName=Root}" Value="False">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ItemsControl.Style>
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Margin="4,0,0,0"
                        Padding="3,3"
                        CornerRadius="12"
                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <Border.ToolTip>
                        <ToolTip Padding="0">
                            <Border Padding="10,8">
                                <StackPanel Orientation="Horizontal">
                                    <controls:CommitIdenticon Size="14"
                                                              Margin="0,0,6,0"
                                                              VerticalAlignment="Center"
                                                              AvatarKey="{Binding AvatarKey}" />
                                    <TextBlock VerticalAlignment="Center"
                                               FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                               Text="{Binding Name}" />
                                    <TextBlock Margin="6,0,0,0"
                                               VerticalAlignment="Center"
                                               FontSize="10"
                                               Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                                        <Run Text="&lt;" /><Run Text="{Binding Email, Mode=OneWay}" /><Run Text="&gt;" />
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </ToolTip>
                    </Border.ToolTip>
                    <controls:CommitIdenticon Size="16" AvatarKey="{Binding AvatarKey}" />
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
