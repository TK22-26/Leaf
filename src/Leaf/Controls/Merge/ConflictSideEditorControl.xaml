<UserControl x:Class="Leaf.Controls.Merge.ConflictSideEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:avalonedit="clr-namespace:Leaf.TextEdit"
             mc:Ignorable="d">
    <Grid>
        <!-- Canvas behind Editor so scrollbar (inside Editor) renders on top of banners -->
        <Canvas x:Name="OverlayCanvas"
                Panel.ZIndex="0"
                ClipToBounds="True"
                IsHitTestVisible="False"/>
        <avalonedit:TextEditor x:Name="Editor"
                               Panel.ZIndex="1"
                               FontFamily="Consolas"
                               FontSize="12.5"
                               ShowLineNumbers="False"
                               LineNumbersForeground="{DynamicResource TextFillColorTertiaryBrush}"
                               HorizontalScrollBarVisibility="Auto"
                               VerticalScrollBarVisibility="Auto"
                               Background="Transparent"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               IsReadOnly="True"
                               Padding="0"/>
    </Grid>
</UserControl>
